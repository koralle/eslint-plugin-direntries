import { defineConfig } from 'oxfmt'

export default defineConfig({
  tabWidth: 2,
  useTabs: false,
  semi: false,
  singleQuote: true,
  printWidth: 100,
  sortImports: true,
  sortPackageJson: true,
})
